import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { QuestionImagePage } from './question-image';

@NgModule({
  declarations: [
    QuestionImagePage,
  ],
  imports: [
    IonicPageModule.forChild(QuestionImagePage),
  ],
})
export class QuestionImagePageModule {}
